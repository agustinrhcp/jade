module Interop exposing (current_time_ms, env_var)

uses Time with
  now: Task(Int, Never)
end

uses ENV with
  fetch: String -> Task(Maybe(String), Never)
end

def current_time_ms() -> Task(Int, Never)
  now()
end

def env_var(key: String) -> Task(Maybe(String), Never)
  fetch(key)
end
